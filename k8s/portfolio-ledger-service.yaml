apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-ledger-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portfolio-ledger-service
  template:
    metadata:
      labels:
        app: portfolio-ledger-service
    spec:
      containers:
        - name: portfolio-ledger-service
          image: spring-stock-microservices-portfolio-ledger-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8300
          envFrom:
            - configMapRef:
                name: spring-stock-config
          env:
            - name: SPRING_DATASOURCE_USERNAME
              value: postgres
            - name: SPRING_DATASOURCE_PASSWORD
              value: postgres
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8300
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: portfolio-ledger-service
spec:
  ports:
    - port: 8300
      targetPort: 8300
  selector:
    app: portfolio-ledger-service
