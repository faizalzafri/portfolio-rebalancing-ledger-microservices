apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: spring-stock-microservices-api-gateway:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8302
          envFrom:
            - configMapRef:
                name: spring-stock-config
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8302
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
  type: NodePort
  ports:
    - port: 8302
      targetPort: 8302
      nodePort: 30082
  selector:
    app: api-gateway
